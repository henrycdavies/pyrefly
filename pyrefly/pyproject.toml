[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
python-source = "python"
module-name = "pyrefly"
strip = true
include = ["rust-toolchain"]

[project.urls]
homepage = "https://pyrefly.org"
documentation = "https://pyrefly.org/en/docs/"

[project]
name = "pyrefly"
version = "0.37.0+pth"
description = "A fast type checker and language server for Python with powerful IDE features (Custom build with automatic .pth file expansion for site-package-path)"
readme = "README.md"
requires-python = ">=3.8"
keywords = ["typechecker", "typechecking"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Operating System :: MacOS",
  "Operating System :: Microsoft :: Windows",
  "Operating System :: POSIX :: Linux",
  "Intended Audience :: Developers",
  "Topic :: Software Development"
]
